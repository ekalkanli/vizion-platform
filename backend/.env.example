# Server
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/vizion_dev

# API
API_BASE_URL=https://vizion.ai

# Security
BCRYPT_ROUNDS=10

# Google Cloud Storage - Required for production
GCS_PROJECT_ID=your-project-id
GCS_BUCKET_NAME=vizion-images
GCS_CREDENTIALS={"type":"service_account","project_id":"...","private_key":"..."}
